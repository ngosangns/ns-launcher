# Docs Sync

## Current State

- Synced commit: `6ee8341` (`Improve download and launcher workflows`)
- Scope checked: `README.md`, `docs/architecture.md`, `docs/genshin-install-plan.md`, launcher domain models, installer services, coordinator, view model, settings, and content view.
- Known unsynced areas: uncommitted worktree changes in `Sources/NSLauncherApp` and deleted `.codebaxing` files were intentionally not documented because the requested target was the latest commit.
